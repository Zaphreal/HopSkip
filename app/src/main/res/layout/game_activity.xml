<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".GameActivity">
    <ImageView
            android:layout_width="85dp"
            android:layout_height="85dp" app:srcCompat="@drawable/frog" android:id="@+id/player"
            android:contentDescription="@string/player"
            android:visibility="visible"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"/>
    <RelativeLayout
            android:orientation="vertical"
            android:layout_width="500dp"
            android:layout_height="500dp"
            android:id="@+id/background" android:visibility="visible"
            app:layout_constraintTop_toTopOf="@+id/player"
            app:layout_constraintBottom_toBottomOf="@+id/player"
            app:layout_constraintVertical_bias="0.501" app:layout_constraintStart_toStartOf="@+id/player"
            app:layout_constraintEnd_toEndOf="@+id/player" app:layout_constraintHorizontal_bias="0.501">
    </RelativeLayout>
</androidx.constraintlayout.widget.ConstraintLayout>